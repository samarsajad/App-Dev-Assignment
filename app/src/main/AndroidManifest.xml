<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/launchicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/launchicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectapp"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>

        <!-- SenderLoginActivity -->
        <activity android:name=".SenderLoginActivity" android:exported="true" />

        <!-- ReceiverLoginActivity -->
        <activity android:name=".ReceiverLoginActivity" android:exported="true" />

        <!-- UploadImageActivity -->
        <activity android:name=".UploadImageActivity" android:exported="true" />

        <!-- ViewImagesActivity -->
        <activity android:name=".ViewImagesActivity" android:exported="true" />

        <!--SplashActivity-->

        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>
